# REDMIME DAILY TASK AUTOMATION

An automation tool to create redmime daily task base on the email provided in the .env file. It will allow also to log the hour(s) consumed and export it into csv file
